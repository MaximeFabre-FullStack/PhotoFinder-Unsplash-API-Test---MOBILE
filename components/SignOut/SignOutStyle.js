import {StyleSheet} from 'react-native';

const SignOutStyle = StyleSheet.create({
  pageContainerSignOut: {
    flex: 1,
  },
  textSignOut: {
    textAlign: 'center',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 'auto',
    marginBottom: 'auto',
    fontWeight: 'bold',
    fontSize: 18,
    color: 'purple',
  },
});

export default SignOutStyle;
